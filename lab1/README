# 支持基本数据类型  
    int型: 可以识别普通形式的十进制整数、也可以识别0x开头的16进制整数、也可以识别0开头的八进制整数
    float型: 可以识别普通的小数形式，也可以识别科学计数法形式
    char型: 支持所有字符的识别，包括普通字符和转义字符
    string型: 支持字符串中包含转义字符

    支持对任意维数的数组的识别
    支持结构体struct的识别

# 支持的语句
    除基本语句外，支持：
    return语句;
    if语句;
    else语句;
    while循环语句;
    for循环语句;
    continue语句;
    break语句;
    switch语句;
    case语句;
    default语句;

# 支持的运算
    除+、-、*、/和&&、||、！外，支持:
    %模运算;
    ++自增运算;
    --自减运算;
    +=加等;
    -=减等;
    *=乘等;
    /=除等;
    %=模等;


# 支持注释
    支持行注释、块注释